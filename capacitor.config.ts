import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nataliesfarm.app',
  appName: "Natalie's Farm",
  webDir: 'dist',
  backgroundColor: '#000000',
  android: {
    backgroundColor: '#000000'
  }
};

export default config;
